local PathfindingService = game:GetService("PathfindingService")
local player = game.Players.LocalPlayer
local AutoFarm = false

-- ฟังก์ชัน AI เดิน (ปรับปรุงใหม่)
local function AI_WalkTo(targetPosition)
    local char = player.Character
    local hum = char and char:FindFirstChild("Humanoid")
    local root = char and char:FindFirstChild("HumanoidRootPart")
    if not hum or not root then return end

    local path = PathfindingService:CreatePath({
        AgentRadius = 3,
        AgentCanJump = true,
        WaypointSpacing = 4
    })

    local success, errorMessage = pcall(function()
        path:ComputeAsync(root.Position, targetPosition)
    end)

    if success and path.Status == Enum.PathStatus.Success then
        local waypoints = path:GetWaypoints()
        for i, waypoint in ipairs(waypoints) do
            if not AutoFarm then break end
            
            hum:MoveTo(waypoint.Position)
            
            if waypoint.Action == Enum.PathWaypointAction.Jump then
                hum.Jump = true
            end

            -- รอจนกว่าจะถึงจุดย่อย (Time out 1 วิ เพื่อความไว)
            local arrived = hum.MoveToFinished:Wait(1)
        end
    else
        -- ถ้า AI คำนวณทางไม่ได้ ให้ใช้การเดินตรงๆ (Fallback)
        hum:MoveTo(targetPosition)
    end
end

-- ฟังก์ชันรันงาน
local function StartBananaJob()
    for _, v in pairs(workspace:GetDescendants()) do
        if v:IsA("ProximityPrompt") and v.ActionText == "Interact" then
            -- หาตำแหน่งที่แน่นอนของ Prompt
            local targetPos = v.Parent:IsA("BasePart") and v.Parent.Position or v.Parent:GetModelCFrame().Position
            
            print("AI กำลังเดินไปหาจุดฟาร์ม...")
            AI_WalkTo(targetPos)
            
            task.wait(0.2)
            fireproximityprompt(v)
            task.wait(1)
            break
        end
    end
end

-- ระบบ Loop
task.spawn(function()
    while true do
        task.wait(1)
        if AutoFarm then
            local success, err = pcall(StartBananaJob)
            if not success then warn("Farm Error: " .. err) end
        end
    end
end)

local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()

local Window = Fluent:CreateWindow({
    Title = "Felon HUB | Thaiban City ",
    SubTitle = "test version ",
    TabWidth = 160,
    Size = UDim2.fromOffset(450, 350),
    Acrylic = true,
    Theme = "VSC Dark High Contrast",
    MinimizeKey = Enum.KeyCode.LeftControl
})

local FarmTab = Window:AddTab({ Title = "Farm Settings", Icon = "settings" })

-- Toggle เปิด/ปิดระบบ AI Farm
FarmTab:AddToggle("AI_BananaToggle", {
    Title = "AI Auto Farm (Pathfinding)", 
    Description = "ใช้ AI คำนวณทางเดินที่โล่งเพื่อไปฟาร์มกล้วย",
    Default = false,
    Callback = function(Value)
        AutoFarm = Value
    end
})

-- ปุ่มวาร์ป (สำรองไว้เผื่อฉุกเฉิน)
FarmTab:AddButton({
    Title = "Teleport To Farm",
    Callback = function()
        local char = player.Character
        local root = char and char:FindFirstChild("HumanoidRootPart")
        if root then
            root.CFrame = CFrame.new(166.50035, 9.57122, 241.507)
        end
    end
})
